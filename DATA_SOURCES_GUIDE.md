# 数据源配置指南

> 如何添加更多数据源，特别是心理学和教育学垂直领域

---

## 📊 当前数据源

### 总计: 22个数据源

| 类型 | 数量 | 说明 |
|------|------|------|
| 基础数据源 | 14个 | 通过 RSSHub 获取 |
| 扩展数据源 | 8个 | 直接 RSS 订阅 |

---

## 🔧 如何添加新数据源

### 方法一：RSSHub 路由

**适用**: 中文平台、国内媒体

**步骤**:
1. 查找 RSSHub 路由文档: https://docs.rsshub.app
2. 测试路由是否可用:
   ```bash
   curl -s "http://localhost:1200/{路由}" | grep "<item>"
   ```
3. 添加到 `hotnews_storage.py` 的 `SOURCES` 字典

**示例**:
```python
"new_source": {
    "name": "新数据源",
    "platform": "平台名",
    "url": "/path/to/route",
    "category": "分类"
}
```

### 方法二：直接 RSS 订阅

**适用**: 国际网站、专业媒体

**步骤**:
1. 查找网站的 RSS 链接（通常在页面底部或 `/feed` `/rss` 路径）
2. 测试 RSS 是否可用:
   ```bash
   curl -s "RSS链接" | grep "<item>"
   ```
3. 添加到 `extended_sources.py` 的 `DIRECT_RSS_SOURCES` 字典
4. 运行测试:
   ```bash
   python3 extended_sources.py --test
   ```

---

## 🎯 心理学/教育学数据源推荐

### 已验证可用

| 数据源 | 分类 | RSS链接 | 状态 |
|--------|------|---------|------|
| BBC教育新闻 | 教育 | `https://feeds.bbci.co.uk/news/education/rss.xml` | ✅ |
| Inside Higher Ed | 教育 | `https://www.insidehighered.com/rss.xml` | ✅ |
| ScienceDaily心智 | 心理学 | `https://www.sciencedaily.com/rss/mind_brain.xml` | ✅ |
| Phys.org科学 | 科学 | `https://phys.org/rss-feed/` | ✅ |

### 待验证/需要配置

| 数据源 | 分类 | 获取方式 |
|--------|------|----------|
| 简单心理 | 心理学 | 需微信公众号或网站爬虫 |
| 壹心理 | 心理学 | 需微信公众号或网站爬虫 |
| 中国教育报 | 教育 | 需配置RSSHub |
| 芥末堆 | 教育科技 | 需配置RSSHub |
| Psychology Today | 心理学 | 被反爬虫限制 |
| EdSurge | 教育科技 | 需测试 |

---

## 🛠️ 自定义爬虫方案

对于没有 RSS 的网站，可以编写自定义爬虫：

### 示例：简单心理爬虫

```python
def crawl_jiandanxinli():
    """爬取简单心理文章"""
    import requests
    from bs4 import BeautifulSoup
    
    url = "https://www.jiandanxinli.com/articles"
    headers = {"User-Agent": "Mozilla/5.0..."}
    
    resp = requests.get(url, headers=headers)
    soup = BeautifulSoup(resp.text, 'html.parser')
    
    items = []
    for article in soup.select('.article-item'):
        title = article.select_one('h2').text
        link = article.select_one('a')['href']
        items.append({
            "title": title,
            "url": link,
            "source_id": "jiandanxinli",
            "source_name": "简单心理",
            "category": "心理学"
        })
    
    return items
```

### 需要爬虫的网站

| 网站 | URL | 难度 |
|------|-----|------|
| 简单心理 | jiandanxinli.com | 中等 |
| 壹心理 | xinli001.com | 中等 |
| KnowYourself | knowyourself.cc | 中等 |
| 中国教育新闻网 | jyb.cn | 较难 |
| 芥末堆 | jiemodui.com | 中等 |

---

## 📝 添加新数据源清单

**添加前检查**:
- [ ] 是否有 RSS/Atom 订阅链接？
- [ ] RSSHub 是否支持？
- [ ] 是否需要登录/Cookie？
- [ ] 是否有反爬虫机制？

**添加后测试**:
- [ ] 测试采集是否成功
- [ ] 检查数据格式是否正确
- [ ] 验证去重是否生效
- [ ] 确认定时任务是否包含

---

## 🚀 快速添加命令

```bash
# 测试新RSS源
curl -s "RSS链接" | grep -oP '(?<=<title>)[^<]+' | head -5

# 测试RSSHub路由
curl -s "http://localhost:1200/路由" | grep -oP '(?<=<title>)[^<]+' | head -5

# 更新扩展源配置
python3 extended_sources.py --test

# 全量采集测试
python3 hotnews_storage.py --crawl
```

---

## 📊 数据源扩展方向

### 短期可添加
- 更多国际教育媒体
- 专业学术期刊
- OpenAI/DeepMind 博客

### 中期需要开发
- 微信公众号爬虫
- 专业网站爬虫
- 学术论文 API

### 长期目标
- 用户自定义数据源
- 自动发现 RSS
- 数据源健康监控

---

*最后更新: 2026-02-21*